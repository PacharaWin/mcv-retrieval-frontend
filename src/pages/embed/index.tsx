import React from 'react';
import FileUpload from '../../components/FileUpload';

const Home: React.FC = () => {
    return (
        <div>
            <h1>File Upload</h1>
            <FileUpload />
        </div>
    );
};

export default Home;
